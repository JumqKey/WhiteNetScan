[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "whitenetscan"
version = "1.0.0"
description = "WhiteNetScan Powered By Miya Service (safe CIDR stats + optional domain checks)"
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[project.scripts]
scan = "whitenetscan.cli:main"
